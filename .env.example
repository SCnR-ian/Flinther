# Copy to .env and fill in your values
VITE_API_URL=http://localhost:8000/api

# ── Club (multi-tenancy) ──────────────────────────────────
# Set to the subdomain of the club this frontend belongs to.
# Used on localhost and Vercel preview deploys (no real subdomain).
VITE_CLUB_SUBDOMAIN=epping
